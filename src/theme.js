import { createTheme } from '@material-ui/core/styles';

const theme = {
  palette: {
    primary: {
      main: '#fd8c25',
      light: 'rgba(251,174,31,0.7)',
      dark: 'rgb(251,174,31)',
    },
  },
};

export default createTheme(theme);
